class ItemCardModel {
  ItemCardModel({
    required this.id,
    required this.image,
    required this.isMatched,
    required this.isflipped,
  });
  int id;
  String image;
  bool isflipped;
  bool isMatched;
}
